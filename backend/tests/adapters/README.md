# Adapter Tests

Use this folder for market-data, broker-import, LLM-provider, and TradingAgents adapter contract tests.

Adapter tests should use mocks and fixtures by default. Tests that touch real external systems must be explicitly marked `external` and should not run in the default suite.
