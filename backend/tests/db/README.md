# Database Tests

Use this folder for database connection, SQLAlchemy persistence, and Alembic migration verification tests.

Database tests should use synthetic data only and skip cleanly when the configured local test database is unavailable.
