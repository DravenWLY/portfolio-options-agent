# Regression Tests

Use this folder for tests that lock in fixes for previously observed bugs.

Each regression test should describe the bug it prevents and use the smallest synthetic fixture that reproduces the issue.
