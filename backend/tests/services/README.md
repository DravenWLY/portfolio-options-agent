# Service Tests

Use this folder for service-layer tests that exercise application behavior without going through HTTP routes.

External providers, LLMs, broker integrations, and TradingAgents must be mocked by default.
