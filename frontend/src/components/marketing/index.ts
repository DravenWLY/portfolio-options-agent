/**
 * P20A-T4 — Marketing placeholder shared components.
 *
 * Translated (not pasted) from the typed prototype screens in
 *   design/prototype/portfolio-copilot-modern-desk/Portfolio Copilot/screens/
 *
 * Backend-/data-agnostic. Static placeholder content only. No backend
 * client calls, no localStorage, no execution / order-placement UI.
 */
export { default as SectionH } from "./SectionH";
export { default as MarketingFooter } from "./MarketingFooter";
